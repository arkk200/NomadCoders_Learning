def say_hello(user_name): # 변수처럼 쓰면 된다. 이게 전달인자(parameter)이다.
    # 쉼표(,)를 이용하여 문자열 사이에 데이터를 삽입할 수 있다.
    print("hellow", user_name, "how r u?")

# 함수에 들어가는 데이터를 인자(argument)라고 한다.
say_hello("arkk")
say_hello("nico")
say_hello("lynn")
say_hello("lewis")
say_hello("ralph")